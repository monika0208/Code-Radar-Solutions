#include<stdio.h>

void reverse(arr[],a,b){
    while(){
    temp=a;
    a=b;
    b=temp;
    a++;
    b--;
    }

}


void rotate(arr[]){
    reverse(arr[],0,n-1);
    reverse1(arr[],0,k-1);
    reverse(arr[],k,n-1);
}

int main(){
    int n;
    scanf("%d",&n);

    for(int i=1;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int k;
    scanf("%d",&k);
    rotate()
    return 0;
}